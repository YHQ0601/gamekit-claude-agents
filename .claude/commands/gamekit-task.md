Use the canonical workflow in `.claude/skills/gamekit-task/SKILL.md`.

Apply it to the user's current task-management request. Create, split, refine, claim, block, close, or audit task cards under `docs/tasks/` without implementing the tasks.
