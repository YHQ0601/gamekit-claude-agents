Use the canonical workflow in `.claude/skills/gamekit-unity-yaml-context/SKILL.md`.

Apply it to the user's current request. Summarize Unity `.prefab`, `.unity`, or `.asset` YAML before reading full raw serialized content. Keep the operation read-only; do not edit Unity YAML files with this command.
