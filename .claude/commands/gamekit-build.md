Use the canonical workflow in `.claude/skills/gamekit-build/SKILL.md`.

Apply it to the user's current request. Implement the smallest useful game change, follow core rules and the active engine profile, avoid unrelated refactors, and run or propose relevant validation.
